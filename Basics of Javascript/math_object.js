//Math object

let val;
val=Math.PI;   //Pi (3.14)
val=Math.E;    //euler's num
val=Math.round(2.4);  //rounds off the value
val=Math.pow(8,2); //returns powers i.e 8^2
val=Math.floor(8.4);
val=Math.ceil(2.6);  //Ceil 
val=Math.sqrt(4);     //Square root of a num
val=Math.abs(-4);   //returns postive value of the num
val=Math.min(2,3,453,2,2.424);   //console logs Minimum number
val=Math.max(2,3,4,5,554,32,2); //console logs Maximum number 
val=Math.random();   //random number from 0 to 1
val=Math.floor(Math.random() * 10 + 0);  //random numbers between 0 to 10

console.log(val)